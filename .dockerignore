# Git
.git
.gitignore
.gitattributes

# Secrets (mount via docker-compose volume, not in image)
.env
.env.local
.env.*.local
config.json
data/

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Go test/build artifacts
*.test
*.out
coverage.txt

# CI/CD
.github/
.gitlab-ci.yml
.releaserc.json

# Docs (kept locally, not needed in image)
*.md
!README.md
docs/
plans/

# Frontend leftovers from upstream
webui/
node_modules/
app/
api/
start.mjs
vercel.json
zeabur.yaml

# Tests (slim build doesn't run them inside image)
tests/
